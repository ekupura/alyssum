version: '3'
services:
  api:
    build: ./api
    container_name: web-api
    working_dir: /mnt/web/server/
    ports:
      - "55000:5000"
    volumes:
      - ./volume/server:/mnt/web/server
    tty: true
    command: ["uvicorn", "server:app", "--reload", "--host", "0.0.0.0", "--port", "5000"]

  node:
    build: ./node
    container_name: web-node
    working_dir: /mnt/web/client
    ports:
      - "53000:3000"
    tty: true
    #volumes:
      #- ./volume/web/server:/mnt/web/server/
      #- ./volume/web/client:/mnt/web/client/
    #command: ["./wait-for-it.sh", "api:5000", "--", "npm", "start"]
    #command: ["npm", "start"]
 
